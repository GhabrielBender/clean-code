import { Router } from '@/presentiation/components'
import React from 'react'
import ReactDOM from 'react-dom'
import '@/presentiation/styles/global.scss'

ReactDOM.render(
  <Router />,
  document.getElementById('main')
)